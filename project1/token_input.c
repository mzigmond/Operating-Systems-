#include <stdio.h>

void tokenize (char *input, int *token_count, char ***token_vector){
    char copy_string = strdup(input); // makes a copy of the string in the heap because strsep will edit the original string 
    
}

int main(){
    char buffer[100]; // Stores the string inputted 
    printf("Enter a line of text: ");
    if (fgets(buffer, sizeof(buffer), stdin != NULL)) return 1;

}
